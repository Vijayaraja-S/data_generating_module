package org.example.random_data_generater.bean.enums;

public enum TypeData {
     DATE,
     ADDRESS,
     CITY,
     STREET_NAME,
     ZIP_CODE,
     ROW_NUMBER,
     EMAIL,
     GENDER,
     FIRST_NAME,
     LAST_NAME,
     FULL_NAME,
     PHONE_NUMBER,
     PASSWORD_HASHCODE,
     SHA_256_HASHCODE,
     CREDIT_CARD

}
